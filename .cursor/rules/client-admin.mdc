---
description: Vite React client and admin apps — Clerk publishable key, Task builds
globs: apps/client/**,apps/admin/**
alwaysApply: false
---

# Client & admin UI

- **Client** (`apps/client`): player-facing Vite + React + Clerk.
- **Admin** (`apps/admin`): ops dashboard; same Clerk app; requires user public metadata `{ "role": "admin" }`.

READMEs: [`apps/client`](../../apps/client) (via root README), [`apps/admin/README.md`](../../apps/admin/README.md).

## Commands

- Dev: `npm run dev` / `task admin:dev` (or root npm scripts).
- Build: `task client:build` / `task admin:build`
- Deploy: `task client:deploy` / `task admin:deploy` (bake `VITE_API_URL` from Terraform `api_url`)

## Conventions

- Only `VITE_CLERK_PUBLISHABLE_KEY` in Vite apps — never the secret key.
- Feature folders (`core`, `auth`, `cards`, `decks`, `chat` on client; `core`, `auth`, `etl-syncs`, `recommendations` on admin) with kind subfolders and a root `index.ts` barrel. Shared chrome lives in `packages/ui` (`ui/theme`, `ui/auth`, `ui/health`, `ui/mana`).
- Prefer existing UI primitives under `src/core/ui` (shadcn-style); avoid one-off card chrome in heroes/marketing unless the design system requires it.
- Match Prettier (no semicolons, single quotes) and ESLint flat config at repo root.
- ESLint ignores generated `core/ui/**`; don’t fight shadcn output unless necessary.
