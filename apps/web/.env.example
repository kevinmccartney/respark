# Copy to .env.local and run `clerk env pull` from apps/web to fill values.
VITE_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=

# Optional; defaults to http://localhost:3000 for local API
# VITE_API_URL=http://localhost:3000
