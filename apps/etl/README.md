# respark ETL

MTG data pipeline (Scryfall catalog + MTGJSON enrichment).

**Docs:** see the monorepo guide under [`docs/etl/`](../../docs/etl/overview.md).

```bash
task etl -- sync --catalog --limit 10
task etl -- report
```
